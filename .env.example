# Browser-visible URL for the Socket.IO backend.
# Leave empty for local custom-server development (`npm run dev`) where Next and Socket.IO share one origin.
NEXT_PUBLIC_SOCKET_URL=

# Comma-separated frontend origins allowed to connect to the Socket.IO backend in production.
# Example: https://debug-duel-arena.vercel.app
ALLOWED_ORIGINS=
